name: Check Repositories

on:
  schedule:
    - cron: '0 0 * * *'  # Runs every day at midnight
  workflow_dispatch:  # Allows manual triggering of the workflow

jobs:
  check-repositories:
    uses: Telucero/workflows/.github/workflows/check-repositories.yml@main  # Calling the centralized workflow
    with:
      # Any input parameters can go here, though in this case there are none
    secrets:
      REPO_SCOPED_TOKEN: ${{ secrets.REPO_SCOPED_TOKEN }}  # Passing the secret here
