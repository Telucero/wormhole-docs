name: Check Repositories

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  check-repositories:
    uses: Telucero/workflows/.github/workflows/check-repositories.yml@main
    secrets:
      github_token: ${{ secrets.GITHUB_TOKEN }}
